server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name sukaalicheck.com;

    ssl_certificate     /etc/letsencrypt/live/sukaalicheck.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sukaalicheck.com/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    # RSC payload sub-files for result pages (e.g. /predict/result/pred_xxx/__next._full.txt)
    location ~ "^/predict/result/[^/]+/(__next\.[^/]+)$" {
        try_files $uri /predict/result/_/$1;
    }

    # Flat RSC payload files for result pages (e.g. /predict/result/pred_xxx.txt)
    location ~ "^/predict/result/[^/]+\.txt$" {
        try_files $uri /predict/result/_.txt;
    }

    # HTML shell for any result-page ID not in the static export
    location /predict/result/ {
        try_files $uri $uri.html $uri/ /predict/result/_/index.html;
    }

    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://172.18.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
#end